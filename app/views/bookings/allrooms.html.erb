<p id="notice"><%= notice %></p>

<h2>Current and upcoming bookings</h2>

<table class="table">
  <thead>
    <tr>
      <th>Room number</th>
      <th>User ID</th>
      <th>Datetime</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bookings.each do |booking| %>
      <tr>
        <td><%= booking.room_number %></td>
        <td><%= booking.user_id %></td>
        <td><%= booking.datetime %></td>
        <td><%= link_to 'Cancel booking', booking, method: :delete, data: { confirm: 'Are you sure to cancel?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

